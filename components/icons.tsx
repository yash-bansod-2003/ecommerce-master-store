import { Trash2, X, LucideIcon, Expand, ShoppingBag } from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
    close: X,
    delete: Trash2,
    expand: Expand,
    cart: ShoppingBag,
};
